class Person
instance variables
  participates:set of (FashionShow);
  attendes:set of (Event);
  presents:set of (Presentation);
  directs:set of (RunwayShow);
  displays:set of (PieceOfCloth);
  creations:set of (PieceOfCloth);
  name:Utils`string;
  birthdate:?;
  Attribute:Utils`string;
  gender:Utils`gender;
  isHandicapped:bool;

end Person
