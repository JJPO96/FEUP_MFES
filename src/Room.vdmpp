class Room
instance variables
  name:Utils`string;
  localization:Utils`string;

operations
-- constructor
	public Room : Utils`string * Utils`string ==> Room
		Room(n, local) == (
			name := n;
			localization	:= local;
			return self;
		)
end Room
